# VoiceDirectorV2 v1.6.3

## Summary

Release preparation update for the renamed `VoiceDirectorV2` custom-repository plugin entry.

## Highlights

- Renamed the published plugin identity from `VoiceDirector` to `VoiceDirectorV2` to avoid colliding with the official Dalamud repository entry.
- Updated the plugin manifest, custom repo JSON, and master repository metadata to use the new `VoiceDirectorV2` internal name.
- Fixed the published image URLs to point at the live branch that actually serves the icon and preview assets.
- Kept the existing plugin behavior unchanged while aligning the packaging and repository metadata for custom repo distribution.

## Notes

- This release is focused on repository and packaging correctness so the plugin can appear reliably in custom repositories.
- Validation should still include a quick in-game smoke test after publishing because the plugin changes a live game config value during territory transitions.
