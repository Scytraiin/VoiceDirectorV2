# Voice Director v1.6.2

## Summary

Metadata and release-target correction release for Voice Director, aligning the published manifest, repository JSON, and release automation with the current `Scytraiin/VoiceDirectorV2` repository.

## Highlights

- Updated the plugin manifest to use the current repository URL, author name, and raw asset links.
- Updated the project metadata so package/project URLs now point at `Scytraiin/VoiceDirectorV2`.
- Normalized the custom repo JSON icon and download-link handling to match the current repository layout more closely.
- Fixed the release preparation script so it derives release download links from the active repo JSON instead of the old repository name.
- Kept the API 15 plugin, test, Docker, and release workflow introduced in `v1.6.1`.

## Notes

- This release is focused on correctness of published metadata and release packaging rather than plugin behavior changes.
- Validation should still include a quick in-game smoke test after publishing because the plugin changes a live game config value during territory transitions.
